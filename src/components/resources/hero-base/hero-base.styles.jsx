// Modules
import styled from "styled-components";

// Shared
import { COLORS } from "../../../assets/styles";

export const ResourcesHeroBaseWrapper = styled.div`
  span {
    color: ${COLORS["deep-danger-color"]};
  }
`;
