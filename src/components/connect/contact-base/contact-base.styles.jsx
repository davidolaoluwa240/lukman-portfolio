// Modules
import styled from "styled-components";

// Shared
import { COLORS } from "../../../assets/styles";

export const ContactBaseWrapper = styled.section`
  margin-top: 2.4rem;
  margin-bottom: 9.6rem;
`;

export const ContactBaseHeading = styled.h6`
  font-size: 2.8rem;
  line-height: 40px;
  font-weight: 400;
  letter-spacing: -1px;
  font-family: "Vollkorn", serif;
  margin-bottom: 2.2rem;
  color: ${COLORS["text-color"]};
`;
